node_modules/
dist/
build/
# .env JEST w repo (domyślne wartości dev) — nadpisuj przez .env.local
.env.local
.env.*.local
*.log
.DS_Store
coverage/
.vite/
# Dźwięki są generowane skryptem (tools/generate_sounds.py), ale trzymamy je
# w repo — obraz produkcyjny buduje się z czystego klona
e2e/.auth-token
test-results/
playwright-report/
*.tsbuildinfo
__pycache__/
