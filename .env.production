# Build produkcyjny: frontend i API są pod tym samym adresem, więc adres API
# zostaje pusty — klient używa origin strony. Wartości VITE_* są wkompilowane
# w bundle, więc nie da się ich podmienić dopiero na serwerze.
VITE_API_URL=
